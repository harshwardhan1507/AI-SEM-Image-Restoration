# Training Configuration Placeholder

# Optimization Hyperparameters
# batch_size:
# epochs:
# learning_rate:
# weight_decay:
# beta1:
# beta2:

# Optimizer & Scheduler
# optimizer: # e.g., "AdamW"
# scheduler: # e.g., "CosineAnnealingLR"
# min_lr:
# warmup_epochs:

# Loss Functions
# loss_type: # e.g., "PSNRLoss", "CharbonnierLoss", "L1Loss"

# Training Settings
# mixed_precision: # true / false
# gradient_clip_val:
# val_freq: # Save/validate every N epochs
# log_freq: # Log metrics every N steps

# Data Configuration
# dataset_path:
# patch_size:
augmentations:
  synthetic_degradation:
    probability: 0.5
    blur_prob: 0.15
    speckle_prob: 1.0
    poisson_prob: 0.0
    gaussian_prob: 0.0
    speckle_sigma_range: [0.0455, 0.2405]
    poisson_scale_range: [100.0, 250.0]
    gaussian_sigma_range: [0.0, 0.0183]

# Checkpoint Management
# checkpoint_dir:
# save_top_k:

metrics:
  psnr: true
  ssim: true
  lpips: false
