# Experiment 007 Configuration: Scaled NAFNet Width 48 SEM Image Restoration - Synthetic Augmentation
experiment_id: "exp007_augmentation"
description: "Scaled NAFNet with width=48 on SEM dataset for 50 epochs testing KLA-physics synthetic augmentation robustness."

system:
  seed: 42
  device: "auto"
  log_dir: "logs/exp007_augmentation"
  checkpoint_dir: "outputs/checkpoints/exp007_augmentation"
  tensorboard_dir: "outputs/tensorboard/exp007_augmentation"

data:
  dataset_dir: "./dataset"
  splits: ["train", "val"]
  train_batch_size: 4
  val_batch_size: 4
  num_workers: 4
  pin_memory: true
  augmentations:
    synthetic_degradation:
      probability: 0.5
      blur_prob: 0.15
      speckle_prob: 1.0
      poisson_prob: 0.0
      gaussian_prob: 0.5  # Ablation test for test-set insurance
      speckle_sigma_range: [0.0455, 0.2405]
      poisson_scale_range: [100.0, 250.0]
      gaussian_sigma_range: [0.0, 0.0183]

model:
  name: "NAFNet"
  img_channel: 1
  width: 48
  middle_blk_num: 1
  enc_blk_nums: [1, 1, 1]
  dec_blk_nums: [1, 1, 1]
  upscale: 2
  drop_out_rate: 0.0

loss:
  name: "charbonnier"
  eps: 1.0e-3
  reduction: "mean"

train:
  epochs: 50
  batch_size: 4
  learning_rate: 1.0e-3
  weight_decay: 1.0e-4
  min_lr: 1.0e-6
  mixed_precision: true
  val_freq: 1
  log_freq: 10
